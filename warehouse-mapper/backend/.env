# Database Configuration
# Format: postgresql+psycopg://username:password@host:port/database_name
# PostgreSQL is running in Docker on port 5434
# Note: Use postgresql+psycopg:// (not postgresql://) for psycopg v3

DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5434/warehouse_mapper

# Security (optional - defaults are in config.py)
# SECRET_KEY=your-secret-key-change-in-production

# CORS Origins (optional - defaults are in config.py)
# BACKEND_CORS_ORIGINS=["http://localhost:3000","http://localhost:5173"]
